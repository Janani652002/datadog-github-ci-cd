name: CI

on:
  push:
    branches:
      - main
  pull_request:

jobs:
  datadog-static-analysis:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install dependencies
        run: yarn install --frozen-lockfile

      - name: Lint Code
        run: |
          yarn lint:ci || true

      - name: Run Datadog Static Analyzer
        uses: DataDog/datadog-static-analyzer-github-action@main
        with:
          dd_api_key: ${{ secrets.DATADOG_API_KEY }}
          dd_app_key: ${{ secrets.DATADOG_APP_KEY }}
          dd_site: us5.datadoghq.com
          static_analysis_enabled: true
          cpu_count: 2
          diff_aware: true

  check-licenses:
    name: Check licenses
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Enable Corepack and Install node
        run: |
          corepack enable
          corepack prepare yarn@3.4.1 --activate
      - uses: actions/setup-node@v4
        with:
          node-version: 20.11.0
      - run: yarn install --immutable
      - run: yarn check-licenses

  lint:
    name: Lint and Format Check
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Enable Corepack and Set Yarn 3.4.1
        run: |
          corepack enable
          corepack prepare yarn@3.4.1 --activate

      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'

      - name: Install dependencies
        run: yarn install --immutable

      - name: Run ESLint + Prettier Check
        run: |
          if ! yarn lint:ci ; then
            echo ""
            echo "❌ ESLint failed with the following errors:"
            jq '.runs[].results' sarif-datadog-ci.sarif || echo "Could not parse SARIF"
            echo ""
            echo "🔗 You can fix most formatting issues by running:"
            echo "   yarn format"
            echo ""
            exit 1
          fi

      - name: Upload SARIF report (for GitHub UI, optional)
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: eslint-sarif
          path: sarif-datadog-ci.sarif
